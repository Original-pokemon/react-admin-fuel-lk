import { css } from '@emotion/react';

export const transactionsStyle = css({
  ".info": {
    display: "flex",
    alignItems: "center",
    gap: "20px",
    marginBottom: "20px",
    button: {
      padding: "5px",
      cursor: "pointer",
    },
  }
})